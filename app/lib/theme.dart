import 'package:flutter/material.dart';

/// Tokens de diseño tomados del prototipo (oscuro, azul, monoespaciado).
const cBg = Color(0xFF0B0B0D); // fondo casi negro
const cCard = Color(0xFF14151A); // superficie de la caja
const cAccent = Color(0xFF3B82F6); // azul eléctrico
const cDim = Color(0xFF8A8F98); // texto secundario
const cBorder = Color(0x14FFFFFF); // borde sutil (blanco ~8%)
const cDanger = Color(0xFFEF4444); // rojo (cerrar sesión / error)
const cOnline = Color(0xFF22C55E); // verde (conectado)
const fontMono = 'monospace';
